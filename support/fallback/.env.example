# Copy to .env and fill in. Same values Vercel has (dashboard → Project Settings → Environment Variables).

# Public (inlined into the client bundle — same as Vercel)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=https://<fallback-url>

# Server-only (never leave the server)
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
NOTIFICATION_EMAIL_FROM=Wah We Doin <notifications@wahwedoin.com>
CRON_SECRET=
